/**
 * Type enum pour les artefacts
 * On a donc ici les 4 artefacts à récupérer
 * + NONE qui siginifie qu'il n'y a pas d'artéfact.
 */

public enum Artefact {
    CRYSTAL, STATUE,
    CALICE, PIERRE, NONE
}
